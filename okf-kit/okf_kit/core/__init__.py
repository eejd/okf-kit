"""Pure core library: deterministic, no network, no randomness.

Submodules: model, parse, validate, links, search, context, index, templates.
`cli` and `mcp` are thin presentation layers over these.
"""
